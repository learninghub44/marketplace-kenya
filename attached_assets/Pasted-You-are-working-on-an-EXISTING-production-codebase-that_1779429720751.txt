You are working on an EXISTING production codebase that has already been cloned.

DO NOT rebuild the project.
DO NOT simplify architecture.
DO NOT delete existing features unless explicitly required.

Your job is ONLY to EXPAND and UPGRADE the system into a MINI JUMIA-LEVEL MULTI-VENDOR MARKETPLACE.

---

# 🧠 CORE OBJECTIVE

Upgrade this project into a scalable African e-commerce marketplace similar to:

* Jumia
* Amazon marketplace model
* Alibaba light marketplace system

The system already exists. You are enhancing it.

---

# 📦 REQUIRED FEATURE EXPANSIONS

## 1. IMAGE SYSTEM (CRITICAL)

Upgrade the system to support:

* Multiple product images per listing
* Image ordering (primary image selection)
* Image upload validation
* Image compression before upload
* Thumbnail generation
* CDN-ready storage
* Secure private storage (no public bucket exposure)
* Lazy loading images on frontend
* Image preview gallery per product
* Drag-and-drop upload (if frontend supports it)

If storage exists, migrate it to a scalable structure:

* Cloudflare R2 OR Supabase Storage (private buckets only)
* Signed URLs for access

---

## 2. MARKETPLACE STRUCTURE (JUMIA MODEL)

Ensure proper separation:

### USERS

* buyers
* sellers
* admins

### SELLERS

* store profiles
* verification status
* product management
* order management
* earnings dashboard

### PRODUCTS

* categories
* subcategories
* brands
* variants (size, color, etc.)
* pricing
* stock quantity
* discounts

### ORDERS

* cart system
* checkout flow
* order status pipeline:
  pending → paid → processing → shipped → delivered → completed → returned

---

## 3. AI AGENT MODIFICATION (IMPORTANT)

There are existing AI agents in the project.

You MUST:

* Improve instead of replacing them
* Ensure they are role-based
* Add marketplace intelligence

### REQUIRED AGENTS:

#### 1. Product Assistant Agent

* Helps sellers write product descriptions
* Auto-tags categories
* Suggests pricing improvements

#### 2. Buyer Assistant Agent

* Recommends products
* Helps search optimization
* Suggests similar products

#### 3. Admin Fraud Agent

* Detects suspicious listings
* Flags duplicate or fake products
* Monitors unusual seller activity

#### 4. Support Agent

* Handles customer complaints
* Suggests responses for tickets
* Auto-classifies support tickets

---

## 4. API ARCHITECTURE (DO NOT BREAK EXISTING)

* Keep current backend structure
* Extend endpoints only
* Add versioning if missing: `/api/v1/`
* Ensure all endpoints support:

  * pagination
  * filtering
  * sorting
  * validation

---

## 5. DATABASE ENHANCEMENTS

DO NOT drop tables.

ONLY add or extend:

Add:

* product_images table (if not exists)
* seller_profiles enhancements
* order_tracking table
* ai_agent_logs table
* audit_logs table (if missing)

Ensure:

* UUID primary keys
* timestamps
* indexes on:

  * product_id
  * seller_id
  * category_id

---

## 6. SECURITY UPGRADES

Ensure:

* JWT authentication is enforced everywhere
* RBAC is active (buyer/seller/admin)
* No public admin routes
* File uploads are validated (MIME + size)
* Rate limiting on auth routes
* No exposed service keys
* Secure cookies for sessions

---

## 7. PERFORMANCE REQUIREMENTS

Add:

* caching layer (Redis if available)
* pagination everywhere
* lazy loading products
* image optimization
* query optimization (avoid N+1 queries)
* background jobs for:

  * image processing
  * notifications
  * email sending

---

## 8. FRONTEND IMPROVEMENTS

Upgrade UI (DO NOT rebuild):

* product gallery UI (like Jumia)
* seller dashboard improvements
* mobile-first layout
* loading skeletons
* smooth transitions
* category browsing UI
* search + filter system

---

## 9. AI SAFETY RULE

AI must NEVER:

* delete working modules
* flatten architecture
* simplify database design
* remove authentication layers

AI MUST:

* extend
* modularize
* improve
* optimize

---

# 🎯 FINAL GOAL

Transform the existing project into a:

👉 MINI JUMIA MULTI-VENDOR MARKETPLACE
👉 IMAGE-RICH PRODUCT SYSTEM
👉 AI-ENHANCED E-COMMERCE PLATFORM
👉 SCALABLE PRODUCTION ARCHITECTURE

WITHOUT REBUILDING FROM SCRATCH
